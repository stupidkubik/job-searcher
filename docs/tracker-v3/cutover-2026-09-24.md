# Application event cutover — 2026-09-24

Status: applied on integration branch `codex/tracker-v3`, then merged to `main`
as PR #21. D-017 selects explicit `event` operations for post-application
lifecycle. No application was submitted and no `application_status` changed.

Post-merge update, 2026-09-24: PR #21 merged as `e263f03`. Its `validate` and
`reject-request-prs` checks passed. The merged tree matches the verified branch
tree; strict validation checked 458 jobs/474 source references, and the tracker
and index freshness checks passed after merge.

Immediately before migration, `git fetch origin` showed the branch 19 commits
ahead of `origin/main` and 0 behind, with a clean working tree. The default
dry-run classified 458 jobs: 413 without application events, 45 eligible for
backfill, 0 blocked. The plan contained 45 `application_submitted` and 8
`rejection_received` events. All use `precision=date`, `source=migration`,
`actor=migration`, `confirmed_by_user=false`. No notes or timestamps were
inferred.

The fresh temporary-copy rehearsal injected a failure after the first
replacement. Recovery restored the exact copied baseline, including absence
of both event files and cutover marker. The subsequent apply atomically
published 53 events and the marker; the second pass found 0 pending jobs.
Existing CSV, source references, application cards and generated views retained
their bytes.

The canonical migration then ran as one maintenance command with a process
local override:

```bash
TRACKER_V3_EVENT_WRITES=1 python3 scripts/maintenance/backfill_application_events.py \
  --apply --cutover --root /Users/evgenii/Desktop/JOB --format json
```

It created 45 per-job JSONL files and
`config/event-ledger-cutover.json` in one recoverable transaction. Afterward:

- 458 jobs and 474 source references pass strict validation;
- 45 event files contain 53 events, with 0 snapshot mismatches;
- a second migration dry-run reports 45 existing jobs and 0 pending events;
- `tracker.md`, all three indexes and the generated operation contract are fresh;
- public event writes are enabled by the marker without an environment override;
- the full suite passes 277 tests on the migrated checkout.

The unchanged `data/jobs.csv` SHA-256 is
`8475a8a52c307ddb6e00e93485fe8dc3d0644a566845dcfb2c850a1f7cac4fd2`;
the unchanged `data/job_sources.csv` SHA-256 is
`6edcdc78613adc68b8044f453ec46dca03df9419831476860503d314a905e803`.
Generated views were checked byte-for-byte as fresh. The current checkout's
`job-0001` timeline shows a date-only migration submission and the unchanged
`applied` snapshot.

Rollback after this commit is `git revert` of the migration commit, restoring
the absence of the 45 files and marker. Since the CSV and generated views were
not changed by migration, no manual data repair is needed. Do not revert only
the marker: doing so would leave a readable ledger but disable its write path.

Gate 1 evidence was complete on this branch before merge. The post-merge update
above records the deployed cutover and validation.
